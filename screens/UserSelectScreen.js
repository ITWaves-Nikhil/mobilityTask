import React from 'react';
import {View, Text} from 'react-native';

const UserSelectScreen = ({route, navigation}) => {
  return (
    <View style={{flex: 1, backgroundColor: 'red'}}>
      <Text>UserSelectScreen</Text>
    </View>
  );
};

export default UserSelectScreen;
