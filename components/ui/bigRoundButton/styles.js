import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    iconContainer: { padding: 10,borderWidth:3,backgroundColor:'red', alignItems:'center',gap:8},
    icon: { height: 20, width: 20, objectFit: 'contain' },
});